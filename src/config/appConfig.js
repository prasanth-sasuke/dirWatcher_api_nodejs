module.exports.appConfig = {
    PORT: 4000,

    //Mysql
    host: "localhost",
    user: "root",
    password: "root123",
    database: "lumel",

    // Define the interval in milliseconds (1 minute in this example)
    intervalInMs: 15 * 1000
}